<ContentDialog
    x:Class="DHTControl.SetUpNewDeviceContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DHTControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SetUpNewDevice"
    PrimaryButtonText="OK"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    >

    <Grid Margin="0,-10,0,0" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="83*"/>
            <ColumnDefinition Width="187*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="11*"/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Right" Text="SSID:" TextWrapping="Wrap" VerticalAlignment="Center" />
        <TextBlock HorizontalAlignment="Right"  Grid.Row="1" Text="PassWord:" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <TextBox x:Name="ssidTextBox" Grid.Column="1" VerticalAlignment="Center"  HorizontalAlignment="Stretch"/>
        <PasswordBox x:Name="wifiPassWordBox"  Grid.Column="1" HorizontalAlignment="Stretch" Grid.Row="1" VerticalAlignment="Center"/>
    </Grid>
</ContentDialog>
